'use client';

// import { BetterAuthProvider } from '@better-auth/react';

export function Providers({ children }) {
  // Return children directly without auth provider for now
  return (
    <>
      {children}
    </>
  );
}